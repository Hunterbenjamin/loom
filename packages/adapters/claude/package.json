{
  "name": "@loom/adapter-claude",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Claude Code adapter: claude agents --json, per-session hooks, and headless runs over the Agent SDK.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.269",
    "@loom/core": "workspace:*",
    "@loom/protocol": "workspace:*",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
